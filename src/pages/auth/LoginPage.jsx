/**
 * LoginPage — placeholder.
 * Intentionally left empty; the login module will be built separately.
 */
const LoginPage = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-surface-light dark:bg-surface-dark">
      <p className="text-slate-500 dark:text-slate-400">Login page — to be implemented.</p>
    </div>
  );
};

export default LoginPage;
